apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress
spec:
  defaultBackend:
    service:
      name: {{ include "router.fullname" . }}
      port:
        number: {{ .Values.service.port }}
